params {
  // Required inputs — set these to your own data locations, e.g. via
  // -params-file or --seg/--pp/... on the command line.
  seg = null
  pp = null
  snp_path = null
  batch_info = null
  conversion_table = null
  sample_info = null
  outdir = null

  // SVclone-specific inputs (cnv_file/pp_file are derived from seg/pp, snv_vcf
  // is read from batch_info, and snv_pyclone is derived from batch_info's
  // path_to_trees/patient, all in main.nf/svclone.nf — both subworkflows read
  // the same Purple outputs and the same batch_info file)
  bam_dir = null
  purple_dir = null
  coverage_info = null
  conversion_script = null
  // Samples at or below this Purple purity are excluded from SVCLONE_SUBWORKFLOW
  min_purity = 0.1

  // R helper scripts (shipped in this repo, resolved relative to the pipeline)
  harmonization_function = "${projectDir}/assets/1_harmonization_function.R"
  tree_conversion_functions = "${projectDir}/assets/2_tree_conversion_functions.R"
  ci_functions = "${projectDir}/assets/3_ci_functions.R"
  clones_comparison_functions = "${projectDir}/assets/5_clones_comparison_functions.R"

  // Environment — override for your own cluster
  alpaca_env = null
  svclone_env = null
  gurobi_home = null
  gurobi_license_file = null
  slurm_queue = 'evmbig'
}

// Environment-specific overrides (gitignored). Copy local.config.example to
// local.config and fill in your own values. Included here (rather than via
// -c on the command line) so params are resolved before env{}/process{}
// below reference them.
if (new File("${projectDir}/local.config").exists()) {
  includeConfig "${projectDir}/local.config"
}

env {
  GUROBI_HOME = params.gurobi_home
  GRB_LICENSE_FILE = params.gurobi_license_file
  LD_LIBRARY_PATH = "${System.getenv('LD_LIBRARY_PATH')}:${params.gurobi_home}/lib"
}

process {
  executor = 'slurm'
  queue = params.slurm_queue
  errorStrategy = 'ignore'
  withName: 'HARMONIZE' {
    conda = params.alpaca_env
    memory = 8.GB
    cpus = 4
  }
  withName: 'REFPHASE' {
    conda = params.alpaca_env
    memory = 8.GB
    cpus = 4
  }
  withName: 'ALPACA' {
    conda = params.alpaca_env
    cpus = 8
    memory = 16.GB
    maxRetries = 2
    maxForks = 2
  }
  withName: 'ANNOTATION' {
    conda = params.svclone_env
    memory = 24.GB
    cpus = 8
  }
  withName: 'COUNT' {
    conda = params.svclone_env
    memory = 12.GB
    cpus = 1
  }
  withName: 'PREPARE_FILTER_INPUT|FILTER_SV' {
    conda = params.svclone_env
  }
  withName: 'FILTER' {
    conda = params.svclone_env
    memory = 8.GB
    cpus = 1
  }
  withName: 'CLUSTERING' {
    conda = params.svclone_env
    memory = 16.GB
    cpus = 1
  }
}

conda.enabled = true

executor {
  name = 'slurm'
  queueSize = 60
}
